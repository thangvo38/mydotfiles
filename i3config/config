# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex-autostart --autostart --environment i3 &

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --ignore-sleep --transfer-sleep-lock -- i3lock --nofork &
#exec --no-startup-id xss-lock --ignore-sleep --transfer-sleep-lock -- betterlockscreen -l dim
#exec --no-startup-id xautolock -time 10 -locker 'betterlockscreen -l dim'
#exec --no-startup-id xautolock -time 10 -locker 'slock'

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet &

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id $HOME/global-scripts/pulseaudio-control --volume-step 5 --volume-max 130 up
bindsym XF86AudioLowerVolume exec --no-startup-id $HOME/global-scripts/pulseaudio-control --volume-step 5 --volume-max 130 down
bindsym XF86AudioMute exec --no-startup-id $HOME/global-scripts/pulseaudio-control togmute
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

#bindsym XF86AudioRaiseVolume exec --no-startup-id $HOME/.config/i3/scripts/volume.sh up
#bindsym XF86AudioLowerVolume exec --no-startup-id $HOME/.config/i3/scripts/volume.sh down
#bindsym XF86AudioMute exec --no-startup-id $HOME/.config/i3/scripts/volume.sh mute

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
#bindsym $mod+q kill
bindsym $mod+Shift+q exec --no-startup-id bash -c '[[ $(echo -e "Yes\nNo" | dmenu -i -p "Close program?") == "Yes" ]] && i3-msg kill'

# start dmenu (a program launcher)
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+u focus down
bindsym $mod+i focus up
bindsym $mod+l focus right

# change workspace
bindsym $mod+k workspace next
bindsym $mod+j workspace prev

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left focus left
bindsym $mod+Shift+Down focus down
bindsym $mod+Shift+Up focus up
bindsym $mod+Shift+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
#bindsym $mod+Shift+Left move left
#bindsym $mod+Shift+Down move down
#bindsym $mod+Shift+Up move up
#bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+Shift+b split v

# enter fullscreen mode for the focused container
bindsym $mod+Escape fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+Shift+s layout stacking
#bindsym $mod+w layout tabbed
#bindsym $mod+e layout toggle split

# Rotate between each layout
bindsym $mod+Down layout toggle splith tabbed splitv
bindsym $mod+Up layout toggle tabbed splith splitv

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+q workspace number $ws1
bindsym $mod+w workspace number $ws2
bindsym $mod+e workspace number $ws3
bindsym $mod+r workspace number $ws4
bindsym $mod+t workspace number $ws5
bindsym $mod+y workspace number $ws6

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Are you sure you want to logout?' -B 'YES!YES!YES' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize grow width 1 px or 1 ppt
        bindsym j resize shrink width 1 px or 1 ppt

        bindsym k resize grow height 1 px or 1 ppt
        bindsym l resize shrink height 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize grow width 1 px or 1 ppt
        bindsym Down resize shrink height 1 px or 1 ppt
        bindsym Up resize grow height 1 px or 1 ppt
        bindsym Right resize shrink width 1 px or 1 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        #bindsym $mod+Ctrl+r mode "default"
}

# Set default layout
workspace_layout tabbed

bindsym $mod+Ctrl+r mode "resize"

####
## NEW CONFIGURATIONS
####
default_border pixel 0
for_window [class="^.*"] border pixel 2 
gaps inner 10 
gaps outer 10

#default_floating_border pixel 1

####
## NEW KEYBOARD SHORTCUTS
####
#bindsye $mod+space exec "~/.config/i3/change-language.sh"
#bindsym $mod+Shift+space exec --no-startup-id "rofi -modi drun,run -show run"
bindsym $mod+Shift+s exec --no-startup-id "rofi -modi drun,run -show drun"
bindsym $mod+Tab exec --no-startup-id "rofi -show window"
#bindsym $mod+Shift+q exec --no-startup-id "systemctl suspend"
#bindsym $mod+Shift+p exec --no-startup-id "slock"
#bindsym $mod+Shift+p exec --no-startup-id "betterlockscreen -l dim"
bindsym $mod+Shift+p exec --no-startup-id "i3lock -e -c 202124 && systemctl suspend"

# Change screen brightness
bindsym XF86MonBrightnessUp     exec light -k -A 10
bindsym XF86MonBrightnessDown   exec light -k -U 10

for_window [class="^.*"] border pixel 0

# Take screenshot
#interactive screenshot by pressing printscreen
#bindsym Print exec gnome-screenshot
#crop-area screenshot by pressing Mod + printscreen
#bindsym $mod+Print exec gnome-screenshot -a
#bindsym $mod+Shift+Print exec gnome-screenshot -a -c


bindsym Print exec maim $HOME/Screenshots/Screenshot_$(date +%s).png
bindsym $mod+Print exec maim -s $HOME/Screenshots/Screenshot_$(date +%s).png
bindsym $mod+Shift+Print exec maim -s | xclip -selection clipboard -t image/png
# Move and show scratchpad
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

bindsym $mod+Ctrl+greater move workspace to output right
bindsym $mod+Ctrl+less move workspace to output left

bindsym --release $mod+c exec --no-startup-id xdotool key --clearmodifiers  Ctrl+Insert && xdotool keyup Control Insert
bindsym --release $mod+v exec --no-startup-id xdotool key --clearmodifiers Shift+Insert && xdotool keyup Shift Insert 

####
## DEFAULT EXEC PROGRAMS
####
## Start daemon job
# DO NOT USE exec_always
#exec --no-startup-id $HOME/.config/i3/scripts/scan-monitor-safe.sh &
exec --no-startup-id sxhkd &
#exec --no-startup-id xbindkeys
exec --no-startup-id ibus-daemon -drx --cache=none &

# Clipboard manager
exec --no-startup-id greenclip daemon &
exec --no-startup-id $HOME/.config/i3/scripts/smart-ibus.sh &
exec --no-startup-id $HOME/.config/i3/scripts/random-wallpaper.sh.sh &
##

# Start xmodmap to disable Caplocks
exec_always --no-startup-id xmodmap $HOME/.Xmodmap &
# Start Picom to enable Alacritty opacity
exec_always --no-startup-id picom &

exec_always --no-startup-id sh ~/.fehbg &

#output "*" bg /usr/share/backgrounds/seal.jpg fill
#exec_always --no-startup-id autorandr --change

# Polybar
exec_always --no-startup-id ~/.config/polybar/launch.sh
#exec_always --no-startup-id waybar
#bar {
#    swaybar_command waybar
#}

## Picture in Picture
for_window [title="Picture-in-Picture"] sticky enable

# Notification daemon
exec_always --no-startup-id dunst -config ~/.config/dunst/dunstrc &

# Mouse speed
exec_always --no-startup-id xset r rate 190 35 &
exec_always --no-startup-id ~/.config/i3/scripts/move-workspace.sh &

#exec_always --no-startup-id 'move workspace $space2 to output DP-3'
#exec_always --no-startup-id 'move workspace $space2 to output DP-3 eDP-1'
#exec_always --no-startup-id 'move workspace $space3 to output DP-3 eDP-1'
#exec_always --no-startup-id 'move workspace $space4 to output DP-3 eDP-1'
#exec_always --no-startup-id 'move workspace $space5 to output eDP-1'

# https://github.com/ValveSoftware/steam-for-linux/issues/1040
for_window [class="^Steam$" title="^Friends$"] floating enable
for_window [class="^Steam$" title="Steam - News"] floating enable
for_window [class="^Steam$" title=".* - Chat"] floating enable
for_window [class="^Steam$" title="^Settings$"] floating enable
for_window [class="^Steam$" title=".* - event started"] floating enable
for_window [class="^Steam$" title=".* CD key"] floating enable
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
for_window [title="^Steam Keyboard$"] floating enable

# Scratchpads
for_window [instance="telegram-desktop"] resize set 25ppt 60ppt
for_window [instance="pavucontrol"] move scratchpad

bindsym Mod4+Shift+u [instance="pavucontrol"] scratchpad show
bindsym Mod4+equal [instance="notion-app"] scratchpad show
bindsym Mod4+Shift+equal [instance="obsidian"] scratchpad show resize set 50ppt 50ppt 
bindsym Mod4+Shift+d [instance="DBeaver"] scratchpad show resize set 50ppt 60ppt
bindsym Mod4+Shift+m [instance="Telegram"] scratchpad show move position 10ppt 10ppt resize set 25ppt 60ppt 
#bindsym Mod4+p [instance="Alacritty"] scratchpad show, resize 800 600
bindsym Mod4+o [instance="Alacritty"] scratchpad show, fullscreen disable floating enable move position 25ppt 15ppt resize set 48ppt 75ppt
bindsym Mod4+p [instance="Alacritty"] scratchpad show, fullscreen disable floating enable move position 0px 0px resize set 100ppt 100ppt
#bindsym Mod4+o [instance="anki"] scratchpad show
#bindsym Mod4+o [instance="anki"] scratchpad show

####
## Autostart application
##
#exec --no-startup-id i3-msg 'workspace 1; exec /usr/bin/telegram-desktop'
#exec --no-startup-id i3-msg 'workspace 2; exec /usr/local/bin/firefox'
exec --no-startup-id i3-msg 'workspace 1; exec /usr/bin/alacritty' &
exec --no-startup-id /usr/bin/pavucontrol &

#exec --no-startup-id "/home/thangvo/app-source/xborder/xborders -c /home/thangvo/app-source/xborder/config.json"

